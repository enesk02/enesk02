package pkg10.tam.bölünen.sayıları.bulmak;
 //n den m ye kadar olan sayılardan 10 a tam bölünen sayıları bulunuz?
 //n başlangıç ve m bitiş sayısı kullanıcıdan alınacaktır.  
import java.util.Scanner; 
public class TamBölünenSayılarıBulmak {

    public static void main(String[] args) {
    int i,m,n;
     Scanner s = new Scanner(System.in);
     System.out.println("başlangıç sayısı :");
      n = s.nextInt();
      
      System.out.println("bitiş sayısı :");
      m = s.nextInt();
       for (i=n;i<m;i++){
           if(i%10==0) System.out.print(i+ "  ");
       }
    }
    
}
